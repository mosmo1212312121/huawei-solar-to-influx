_format_version: "3.0"
_transform: true

services:
  - name: go-service
    url: http://host.docker.internal:8080 # ชื่อต้องตรงกับ --name ของ Go Container
    routes:
      - name: hello-route
        paths:
          - /hello
